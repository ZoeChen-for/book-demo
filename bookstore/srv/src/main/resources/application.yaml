---
spring:
  config.activate.on-profile: default
  datasource:
    url: "jdbc:sqlite:/codeRepo/bookDemo/bookstore/sqlite.db"
    driver-class-name: org.sqlite.JDBC
    hikari:
      maximum-pool-size: 1
  sql:
    init:
      mode: never
